properties([
	parameters([
		extendedChoice(
			name: 'MULTI_SELECT',
			type: 'PT_CHECKBOX',
			value: 'option1,option2,option3',
			description: '请选择多个选项'
		)
	])
])
pipeline {
	agent any
	stages {
		stage() {
			steps {
				echo "Extended Choice 参数值为：${params.MULTI_SELECT}"
			}
		}
	}
}